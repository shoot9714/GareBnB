<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="login">

<!-- selectId(로그인) -->
<select id="selectId" parameterType="hashmap" resultType="hashmap">
<![CDATA[
	SELECT
		MEM_LEVEL,
		MEM_IDX,
		MEM_ID,
		MEM_PW,
		MEM_NAME,
		MEM_PHONE
	FROM 
		MEMBER
	WHERE 
		MEM_LEVEL IN (1,2,3,4)
]]>
</select>

<!-- selectOneId (아이디 찾기, 아이디 찾기 결과) -->
<select id="selectOneId" parameterType="hashmap" resultType="hashmap">
<![CDATA[
	SELECT 
		MEM_IDX, MEM_ID, MEM_PW, MEM_NAME, MEM_PHONE
	FROM 
		MEMBER
	WHERE 
		MEM_NAME = #{MEM_NAME}
		AND
		MEM_PHONE = #{MEM_PHONE}
		AND
		MEM_LEVEL IN (1,2,3,4)
]]>
</select>

<!-- selectOnePw (비밀번호 찾기, 비밀번호 찾기 결과) -->
<select id="selectOnePw" parameterType="hashmap" resultType="hashmap">
<![CDATA[
	SELECT 
		MEM_IDX, MEM_ID, MEM_PW, MEM_NAME, MEM_PHONE
	FROM 
		MEMBER
	WHERE 
		MEM_NAME = #{MEM_NAME}
		AND
		MEM_ID = #{MEM_ID}
		AND
		MEM_PHONE = #{MEM_PHONE}
		AND
		MEM_LEVEL IN (1,2,3,4)
]]>
</select>
</mapper>